import { color, style, timer } from '../../sources';
const flat = {
	sources: {
		color, style, timer,
	},
	structure: {
		type: 'text',
		data: 'timer',
		style: {
			color: 'color',
			fontSize: 100,
		},
	},
};

export default flat;
